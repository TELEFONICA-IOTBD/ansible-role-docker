---

lvm_storage_enable: true
lvm_storage_pv_pesize: '32'
lvm_storage_pv_devices:
  - /dev/vdb
lvm_storage_vg_name: 'docker'
lvm_storage_lv_name: 'lib'
lvm_storage_lv_size: '9g'

docker_daemon_config:
  storage-driver: overlay2
  debug: true
  exec-opts:
    - native.cgroupdriver=systemd
  authorization-plugins:
    - rhel-push-plugin
  bip: "192.168.200.1/24"
  userland-proxy: true
  userland-proxy-path: "/usr/libexec/docker/docker-proxy-current"
  live-restore: true
  runtimes:
    docker-runc:
      path: "/usr/libexec/docker/docker-runc-current"
  default-runtime: docker-runc

registry:
  login:
    auths:
      test.smartdigits.io:
        auth: c21hcnRkaWdpdHNfZHNuOlIyaTk3enBOZTc=
        email: test@dummy.com 
      test2.smartdigits.io:
        auth: c21hcnRkaWdpdHNfZHNuOlIyaTk3enBOZTc=
        email: test@dummy.com 

